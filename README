Checker is multiple implementations of running a hash algorithm over a directory tree of files.

It's designed to compare the performance of multiple different approaches to this problem.

Currently it's using md5.
